# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=true

# Mod properties
mod.version=0.1.0
mod.group=com.kd_gaming1.tooltipoverhaulsb
mod.id=tooltip-overhaul-sb
mod.name=Tooltip Overhaul SB

# Global dependencies
deps.fabric_loader=0.18.6
deps.fabric_api=[VERSIONED]

deps.midnightlib_version=[VERSIONED]
deps.modmenu_version=[VERSIONED]
deps.hm_api_version=[VERSIONED]
deps.uilib_version=[VERSIONED]

publish.modrinth=# Modrinth mod slug
publish.curseforge=# Curseforge numeric mod ID
